text to binary

def text_to_binary(text):
  binary_list =[]
for char in text:
  binary = format(ord(char), '08b') #convert character to binary ITS EIGHT BITS
  binary_list.append(binary)
return ' '.join(binary_list)

#example
if _name_ == "_main_":
  user_input = input("Enter text to convert to binary: ")
  binary_output = text_to_binary(user_input)
  print(f"Binary representation: {binary_output}")
